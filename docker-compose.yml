services:
  database:
    image: mcr.microsoft.com/mssql/server:2025-latest
    container_name: mssql_server
    restart: unless-stopped
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=UmaSenhaForte123
    ports:
      - "1433:1433"
    volumes:
      - mssql_data:/var/opt/mssql

volumes:
  mssql_data:
